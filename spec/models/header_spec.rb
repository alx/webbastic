require File.join( File.dirname(__FILE__), '..', "spec_helper" )

describe Webbastic::Header do

  it "should generate YAML"
  
  it "should structure generated YAML depending on its children"
  
  it "should have a flat YAML structure if there is only one children generation"

end